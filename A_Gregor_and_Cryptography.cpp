#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int p;
        cin >> p;
        cout << 2 << " " << p - 1 << endl;
    }

    return 0;
}
