#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
    string a, b, c;
    cin >> a >> b >> c;
    cout << a.front() << b.front() << c.front() << endl;
    }
    return 0;
}
