#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        long long n, s;
        cin >> n >> s;
        cout << s / (n * n) << endl;
    } 
    
    return 0;
}
